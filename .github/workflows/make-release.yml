name: Make release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

  workflow_dispatch:
    inputs:
      tag_name:
        description: Tag to release (for example v2.1.0)
        required: true
        type: string

permissions:
  contents: write

jobs:
  release-for-github:
    name: Release for GitHub
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      # --------------------------------------------------
      # Resolve release tag
      # --------------------------------------------------
      - name: Resolve and validate release tag
        shell: bash
        run: |
          set -euo pipefail

          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            version_tag="${GITHUB_REF#refs/tags/}"
          else
            version_tag="${{ inputs.tag_name }}"
          fi

          if [[ ! "$version_tag" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Invalid stable release tag: $version_tag"
            echo "Expected format: v2.1.0"
            exit 1
          fi

          echo "VERSION_TAG=$version_tag" >> "$GITHUB_ENV"

          echo "Release tag: $version_tag"

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      # --------------------------------------------------
      # Build all release APKs
      # --------------------------------------------------
      - name: Build release APKs
        shell: bash
        run: |
          set -euo pipefail

          ./gradlew assembleStandardRelease \
            --stacktrace \
            --no-daemon \
            --max-workers=2

          ./gradlew assembleFongmiRelease \
            --stacktrace \
            --no-daemon \
            --max-workers=2

          ./gradlew assembleNoVulkanRelease \
            --stacktrace \
            --no-daemon \
            --max-workers=2

      # --------------------------------------------------
      # Sign and verify release APKs
      # --------------------------------------------------
      - name: Sign and verify release APKs
        shell: bash
        run: |
          set -euo pipefail

          apksigner="$ANDROID_HOME/build-tools/34.0.0/apksigner"

          if [[ -z "${{ secrets.SIGNING_KEYSTORE }}" ||
                -z "${{ secrets.SIGNING_KEY_ALIAS }}" ||
                -z "${{ secrets.SIGNING_STORE_PASSWORD }}" ||
                -z "${{ secrets.KEY_PASSWORD }}" ]]; then
            echo "Stable releases require all signing secrets."
            exit 1
          fi

          test -x "$apksigner"

          printf '%s' "${{ secrets.SIGNING_KEYSTORE }}" |
            base64 --decode > release.keystore

          trap 'rm -f release.keystore' EXIT

          sign_apk() {
            local unsigned="$1"
            local signed="$2"
            local label="$3"

            if [[ ! -f "$unsigned" ]]; then
              echo "Missing expected $label APK: $unsigned"

              find app/build/outputs/apk \
                -type f \
                -name '*.apk' \
                -print || true

              exit 1
            fi

            "$apksigner" sign \
              --ks release.keystore \
              --ks-key-alias "${{ secrets.SIGNING_KEY_ALIAS }}" \
              --ks-pass "pass:${{ secrets.SIGNING_STORE_PASSWORD }}" \
              --key-pass "pass:${{ secrets.KEY_PASSWORD }}" \
              --out "$signed" \
              "$unsigned"

            "$apksigner" verify --verbose "$signed"

            rm -f "$unsigned"
          }

          # Standard
          release_dir="app/build/outputs/apk/standard/release"

          test -d "$release_dir"

          architectures=(
            universal
            arm64-v8a
            armeabi-v7a
            x86
            x86_64
          )

          for abi in "${architectures[@]}"; do
            sign_apk \
              "$release_dir/app-standard-${abi}-release-unsigned.apk" \
              "$release_dir/app-standard-${abi}-release.apk" \
              "standard $abi release"
          done

          # Fongmi
          flavor_dir="app/build/outputs/apk/fongmi/release"

          test -d "$flavor_dir"

          sign_apk \
            "$flavor_dir/app-fongmi-universal-release-unsigned.apk" \
            "$flavor_dir/app-fongmi-universal-release.apk" \
            "fongmi universal release"

          # No Vulkan
          flavor_dir="app/build/outputs/apk/noVulkan/release"

          test -d "$flavor_dir"

          sign_apk \
            "$flavor_dir/app-noVulkan-universal-release-unsigned.apk" \
            "$flavor_dir/app-noVulkan-universal-release.apk" \
            "non-Vulkan universal release"

      # --------------------------------------------------
      # Create tag if it does not already exist
      # --------------------------------------------------
      - name: Create release tag if needed
        shell: bash
        run: |
          set -euo pipefail

          if git ls-remote --exit-code --tags origin \
              "refs/tags/$VERSION_TAG" >/dev/null 2>&1; then

            echo "Tag $VERSION_TAG already exists."

            git fetch origin "refs/tags/$VERSION_TAG:refs/tags/$VERSION_TAG"

          else

            echo "Tag $VERSION_TAG does not exist."
            echo "Creating tag at commit $GITHUB_SHA..."

            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

            git tag "$VERSION_TAG" "$GITHUB_SHA"
            git push origin "$VERSION_TAG"

          fi

      # --------------------------------------------------
      # Stage release assets and checksums
      # --------------------------------------------------
      - name: Stage release assets and checksums
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p release-assets
          : > checksums.txt

          # Standard APKs
          release_dir="app/build/outputs/apk/standard/release"

          architectures=(
            universal
            arm64-v8a
            armeabi-v7a
            x86
            x86_64
          )

          for abi in "${architectures[@]}"; do
            src="$release_dir/app-standard-${abi}-release.apk"
            dst="release-assets/mpvRx-${abi}-${VERSION_TAG}.apk"

            test -f "$src"

            cp "$src" "$dst"

            sha256sum "$dst" | tee -a checksums.txt
          done

          # Fongmi
          src="app/build/outputs/apk/fongmi/release/app-fongmi-universal-release.apk"
          dst="release-assets/mpvRx-fongmi-${VERSION_TAG}.apk"

          test -f "$src"

          cp "$src" "$dst"

          sha256sum "$dst" | tee -a checksums.txt

          # No Vulkan
          src="app/build/outputs/apk/noVulkan/release/app-noVulkan-universal-release.apk"
          dst="release-assets/mpvRx-no-vulkan-${VERSION_TAG}.apk"

          test -f "$src"

          cp "$src" "$dst"

          sha256sum "$dst" | tee -a checksums.txt

      # --------------------------------------------------
      # Prepare release notes
      # --------------------------------------------------
      - name: Prepare release notes
        shell: bash
        run: |
          set -euo pipefail

          python3 - <<'PY'
          from pathlib import Path

          changelog = Path('CHANGELOG.md').read_text(
              encoding='utf-8'
          ).splitlines()

          section = []
          in_latest = False

          for line in changelog:
              if line.startswith('## '):
                  if in_latest:
                      break
                  in_latest = True

              if in_latest:
                  section.append(line)

          latest = '\n'.join(section).strip()

          if not latest:
              raise SystemExit(
                  'Could not find latest changelog section'
              )

          variant_guide = """
          ### APK variants

          All variants contain the same mpvRx app version and features,
          with their native backends built from
          [mpvlib Android](https://github.com/Riteshp2001/mpvlibAndroid).
          Choose the package that best matches your device:

          - **Standard** (`mpvRx-<architecture>-<version>.apk`):
            Recommended for most devices. Available as Universal and
            architecture-specific APKs with the standard Vulkan-capable
            playback backend.

          - **FongMi** (`mpvRx-fongmi-<version>.apk`):
            Universal package using the FongMi native profile for Vulkan,
            direct MediaCodec, and Dolby Vision playback.

          - **Non-Vulkan** (`mpvRx-no-vulkan-<version>.apk`):
            Universal OpenGL-only package for devices without compatible
            Vulkan support or with unstable legacy GPU drivers.
          """.strip()

          Path('release-notes.md').write_text(
              latest +
              '\n\n---\n' +
              variant_guide +
              '\n\n### SHA-256 checksums\n\n```text\n',
              encoding='utf-8',
          )
          PY

          cat checksums.txt >> release-notes.md

          printf '%s\n' '```' >> release-notes.md

      # --------------------------------------------------
      # Create GitHub Release
      # --------------------------------------------------
      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.VERSION_TAG }}
          name: mpvRx ${{ env.VERSION_TAG }}
          body_path: release-notes.md
          generate_release_notes: true
          draft: false
          prerelease: false
          fail_on_unmatched_files: true
          files: release-assets/*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
